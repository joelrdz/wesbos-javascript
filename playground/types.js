/* eslint-disable */
/*
const name = 'Albus';
const middle = "Severus";
const last = `Potter`;

const sentence = "she's so \"cool\"";
const sentence2 = `she's so "cool"`;

const song = `Ohhh

ya

I like
pizza`;

// const hello = 'hello my name is ' + name + ". Nice to meet you";

// let hello2 = 'Hello my name is ';
// hello2 = hello2 + name;
// hello2 = hello2 + '. nice to meet you';

const hello = `Hello my name is ${name}. Nice to meet you. I am ${1 + 99} years old.`;

const html = `
  <div>
    <h2>${name}</h2>
    <p>${hello}</p>
  </div>
`;

document.body.innerHTML = html;
*/

/*
const age = 30.5;
const name = 'jon';

console.log(typeof age); // number
console.log(typeof name); // string
console.log(typeof 123); // number
console.log(typeof '456'); // string

const a = 10;
const b = 20;

console.log(a + b); // 30
console.log(a - b); // -10
console.log(a * b); // 200
console.log(a / b); // 0.5

console.log(1 + '1') // '11' (string)
console.log('10' * '10') // 100 (number)

console.log(Math.round(20.5)); // 21
console.log(Math.round(20.2)); // 20
console.log(Math.floor(20.9)); // 20
console.log(Math.ceil(20.1)); // 21
console.log(Math.random()); // random value between 0-1

const skittles = 20;
const kids = 3;
const eachKidGets = Math.floor(skittles / kids);
const dadGets = skittles % kids;
console.log(`Each kid gets ${eachKidGets} skittles.`);
console.log(`Dad gets only ${dadGets} skittles.`);

// const price = 10.34;
const price = 1034;

console.log(1000 ** 2000); // Infinity
console.log(-(1000 ** 2000)); // -Infinity
console.log(typeof Infinity); // number
console.log(typeof -Infinity); // number

console.log(typeof 10 - 'dog'); // NaN
console.log(typeof NaN); // Number
*/

/*
const person = {
  first: 'Jon',
  last: 'Snow',
  age: 30,
};

console.log(person.first); // 'Jon'
console.log(person.age); // 30
*/

/*
let dog;
console.log(dog); // undefined
dog = 'snickers';
console.log(dog); // snickers

let somethingUndefined;
const somethingNull = null;

const cher = {
  first: 'cher',
};

const teller = {
  first: 'Raymond',
  last: 'Teller',
};

teller.first = 'teller';
teller.last = null;

console.log(cher.last); // undefined
console.log(teller.last); // null
*/

const isDrawing = false;

const age = 18;
const ofAge = age > 19;
console.log(ofAge); // false

let time = 100;
let time2 = 100;

console.log(time === 100); // true
console.log(time === 10); // false
console.log(time === time2); // true

console.log(10 === 10); // true
console.log(10 == 10); // true
console.log('10' == 10); // true
console.log('10' === 10); // false
